{
  "name": "ulasim-veri-servisi-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd web-ui && npm run dev",
    "build": "cd web-ui && npm install && npx vite build",
    "test": "cd web-ui && npm test"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/react": "^14.1.2",
    "@testing-library/user-event": "^14.5.2",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5"
  }
}
